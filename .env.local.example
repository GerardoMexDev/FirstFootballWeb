# Copiar a .env.local y completar. NUNCA subir .env.local al repo.

# --- Supabase (cliente: seguras de exponer al navegador) ---
NEXT_PUBLIC_SUPABASE_URL=https://<PROJECT_REF>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>

# --- Solo servidor (Edge Functions / scripts). NO usar en el navegador. ---
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# --- API-Football (solo en Edge Functions, nunca en el cliente ni en el repo) ---
API_FOOTBALL_KEY=<api-key>

# --- Dominio interno que la app agrega al nombre de login ---
NEXT_PUBLIC_DOMINIO_LOGIN=footballfirst.uy
